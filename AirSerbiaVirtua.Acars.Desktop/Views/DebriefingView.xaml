<UserControl x:Class="AirSerbiaVirtua.Acars.Desktop.Views.DebriefingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:AirSerbiaVirtua.Acars.Desktop.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DebriefingViewModel}"
             d:DesignHeight="700" d:DesignWidth="980"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="Tile" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource Brand.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="18,16" />
        </Style>
        <Style x:Key="TileLabel" TargetType="TextBlock" BasedOn="{StaticResource Label}">
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style x:Key="TileValue" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Brand.Sans}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource Brand.Dark}" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,36">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Debriefing" Style="{StaticResource H1}" />
                    <TextBlock Text="Analysis of your most recent flight." Style="{StaticResource Body}" Margin="0,6,0,0" />
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding RefreshCommand}" Style="{StaticResource RedButton}" Padding="14,8" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center" />
                        <TextBlock Text="Refresh" Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>

            <ProgressBar IsIndeterminate="True" Height="2" Foreground="{StaticResource Brand.Red}"
                         Background="Transparent" BorderThickness="0" Margin="0,16,0,0"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}" />

            <Border Background="{StaticResource Brand.Surface}" BorderBrush="{StaticResource Brand.Border}" BorderThickness="1"
                    CornerRadius="6" Padding="16,14" Margin="0,18,0,0"
                    Visibility="{Binding StatusMessage, Converter={StaticResource NullToCollapsedConverter}}">
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Body}" />
            </Border>

            <StackPanel Visibility="{Binding HasPirep, Converter={StaticResource BoolToVisConverter}}">

                <!-- Hero: flight + score -->
                <Border Background="{StaticResource Brand.Dark}" CornerRadius="8" Padding="28" Margin="0,22,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding FlightNumber}" Foreground="White" FontFamily="{StaticResource Brand.Serif}" FontSize="28" />
                            <TextBlock Text="{Binding LegLabel}" Foreground="#E6FFFFFF" FontSize="15" Margin="0,2,0,0" />
                            <TextBlock Style="{StaticResource Body}" Foreground="{StaticResource Brand.MutedOnDark}" Margin="0,8,0,0">
                                <Run Text="{Binding Aircraft, Mode=OneWay}" /><Run Text="   ·   " /><Run Text="{Binding DateLabel, Mode=OneWay}" />
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Text="SCORE" Foreground="{StaticResource Brand.MutedOnDark}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding Score}" Foreground="White" FontFamily="{StaticResource Brand.Serif}" FontSize="52" />
                                <TextBlock Text="/100" Foreground="{StaticResource Brand.MutedOnDark}" FontSize="18" VerticalAlignment="Bottom" Margin="4,0,0,10" />
                            </StackPanel>
                            <TextBlock Text="{Binding ScoreGrade}" Foreground="{StaticResource Brand.Red}" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Right" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Landing verdict -->
                <Border Style="{StaticResource Tile}" Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="LANDING" Style="{StaticResource TileLabel}" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding LandingRate}" Style="{StaticResource TileValue}" />
                            <TextBlock Text="{Binding LandingVerdict}" Style="{StaticResource Body}" VerticalAlignment="Center" Margin="14,0,0,0" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Metrics -->
                <UniformGrid Columns="4" Margin="0,12,0,0">
                    <Border Style="{StaticResource Tile}" Margin="0,0,12,0">
                        <StackPanel><TextBlock Text="BLOCK TIME" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding BlockTime}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                    <Border Style="{StaticResource Tile}" Margin="0,0,12,0">
                        <StackPanel><TextBlock Text="AIR TIME" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding AirTime}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                    <Border Style="{StaticResource Tile}" Margin="0,0,12,0">
                        <StackPanel><TextBlock Text="FUEL USED" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding FuelUsed}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                    <Border Style="{StaticResource Tile}">
                        <StackPanel><TextBlock Text="STATUS" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding StatusLabel}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
