<UserControl x:Class="AirSerbiaVirtua.Acars.Desktop.Views.BriefingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:AirSerbiaVirtua.Acars.Desktop.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:BriefingViewModel}"
             d:DesignHeight="700" d:DesignWidth="980"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="Tile" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource Brand.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="18,16" />
        </Style>
        <Style x:Key="TileLabel" TargetType="TextBlock" BasedOn="{StaticResource Label}">
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style x:Key="TileValue" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Brand.Sans}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource Brand.Dark}" />
        </Style>
        <Style x:Key="MetarText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas, 'Courier New'" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource Brand.Dark}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,36">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Briefing" Style="{StaticResource H1}" />
                    <TextBlock Style="{StaticResource Body}" Margin="0,6,0,0">
                        <Run Text="{Binding FlightNumber, Mode=OneWay}" /><Run Text="  ·  " /><Run Text="{Binding LegLabel, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding RefreshCommand}" Style="{StaticResource RedButton}" Padding="14,8" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center" />
                        <TextBlock Text="Refresh" Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>

            <ProgressBar IsIndeterminate="True" Height="2" Foreground="{StaticResource Brand.Red}"
                         Background="Transparent" BorderThickness="0" Margin="0,16,0,0"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}" />

            <!-- No-session / status note -->
            <Border Background="{StaticResource Brand.Surface}" BorderBrush="{StaticResource Brand.Border}" BorderThickness="1"
                    CornerRadius="6" Padding="16,14" Margin="0,18,0,0"
                    Visibility="{Binding StatusMessage, Converter={StaticResource NullToCollapsedConverter}}">
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Body}" />
            </Border>

            <StackPanel Visibility="{Binding HasActiveSession, Converter={StaticResource BoolToVisConverter}}">
                <!-- Route facts -->
                <UniformGrid Columns="4" Margin="0,22,0,0">
                    <Border Style="{StaticResource Tile}" Margin="0,0,12,0">
                        <StackPanel><TextBlock Text="AIRCRAFT" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding Aircraft}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                    <Border Style="{StaticResource Tile}" Margin="0,0,12,0">
                        <StackPanel><TextBlock Text="DISTANCE" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding Distance}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                    <Border Style="{StaticResource Tile}" Margin="0,0,12,0">
                        <StackPanel><TextBlock Text="PLANNED TIME" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding PlannedTime}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                    <Border Style="{StaticResource Tile}">
                        <StackPanel><TextBlock Text="EST. TOTAL FUEL" Style="{StaticResource TileLabel}" /><TextBlock Text="{Binding FuelTotal}" Style="{StaticResource TileValue}" /></StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Fuel breakdown -->
                <Border Style="{StaticResource Tile}" Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="FUEL PLAN (rough estimate)" Style="{StaticResource TileLabel}" />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Style="{StaticResource Body}" Margin="0,0,24,0"><Run Text="Trip: " /><Run Text="{Binding FuelTrip, Mode=OneWay}" /></TextBlock>
                            <TextBlock Style="{StaticResource Body}" Margin="0,0,24,0"><Run Text="Reserve/contingency: " /><Run Text="{Binding FuelReserve, Mode=OneWay}" /></TextBlock>
                            <TextBlock Style="{StaticResource Body}"><Run Text="Total: " /><Run Text="{Binding FuelTotal, Mode=OneWay}" /></TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Weather -->
                <TextBlock Text="Weather" Style="{StaticResource H2}" Margin="0,28,0,12" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Tile}" Margin="0,0,8,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <iconPacks:PackIconMaterial Kind="AirplaneTakeoff" Width="15" Height="15" Foreground="{StaticResource Brand.Red}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding DepIcao}" Style="{StaticResource TileValue}" FontSize="15" Margin="8,0,0,0" />
                            </StackPanel>
                            <TextBlock Text="{Binding DepMetar}" Style="{StaticResource MetarText}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource Tile}" Margin="8,0,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <iconPacks:PackIconMaterial Kind="AirplaneLanding" Width="15" Height="15" Foreground="{StaticResource Brand.Red}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding ArrIcao}" Style="{StaticResource TileValue}" FontSize="15" Margin="8,0,0,0" />
                            </StackPanel>
                            <TextBlock Text="{Binding ArrMetar}" Style="{StaticResource MetarText}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
